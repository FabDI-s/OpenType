[
  { "id": "code_0", "text": "const fetchData = async (url) => { const res = await fetch(url); return res.json(); };" },
  { "id": "code_1", "text": "function debounce(fn, delay) { let timer; return (...args) => { clearTimeout(timer); timer = setTimeout(() => fn(...args), delay); }; }" },
  { "id": "code_2", "text": "const wpm = Math.round((chars / 5) / (elapsed / 60000));" },
  { "id": "code_3", "text": "document.querySelectorAll('.char').forEach((el, i) => el.classList.toggle('active', i === cursor));" },
  { "id": "code_4", "text": "const accuracy = Math.round(((total - errors) / total) * 100);" },
  { "id": "code_5", "text": "git commit -m 'feat: add local typing speed tracker with wpm and accuracy metrics'" },
  { "id": "code_6", "text": "esphome run blind_motor.yaml --device 192.168.1.42" },
  { "id": "code_7", "text": "ollama run llama3.2 --verbose 2>&1 | grep 'eval rate'" },
  { "id": "code_8", "text": "ffmpeg -i input.mp4 -vf 'scale=1920:-1' -crf 18 -preset slow output.mp4" },
  { "id": "code_9", "text": "tailscale up --accept-routes --ssh --operator=$(whoami)" },
  { "id": "code_10", "text": "const sessionId = await saveSession({ mode, textId, wpm, accuracy, durationMs, totalKeystrokes, errors });" },
  { "id": "code_11", "text": "const weakChars = Object.entries(rates).sort((a, b) => b[1] - a[1]).slice(0, 8).map(([ch]) => ch);" },
  { "id": "code_12", "text": "window.dispatchEvent(new CustomEvent('practice', { detail: { practiceText } }));" },
  { "id": "code_13", "text": "const req = indexedDB.open(DB_NAME, DB_VERSION);" },
  { "id": "code_14", "text": "svg.setAttribute('viewBox', `0 0 ${width} ${height}`);" },
  { "id": "code_15", "text": "const linePath = points.map((p, i) => `${i === 0 ? 'M' : 'L'}${p.x},${p.y}`).join(' ');" },
  { "id": "code_16", "text": "const res = await fetch('./data/literatur/zitate.json'); const entries = await res.json();" },
  { "id": "code_17", "text": "document.documentElement.classList.toggle('light');" },
  { "id": "code_18", "text": "const clean = word.replace(/[^a-zA-ZäöüÄÖÜß]/g, '').toLowerCase();" },
  { "id": "code_19", "text": "const elapsedMs = Date.now() - startTime; const seconds = (elapsedMs / 1000).toFixed(1);" },
  { "id": "code_20", "text": "if (e.key === 'Tab') { e.preventDefault(); reset(); return; }" },
  { "id": "code_21", "text": "const spans = charsEl.querySelectorAll('.char'); spans[cursor]?.classList.add('current');" },
  { "id": "code_22", "text": "const tx = db.transaction(['sessions', 'keystrokes'], 'readwrite');" },
  { "id": "code_23", "text": "const pool = texts[mode]; const entry = pool[Math.floor(Math.random() * pool.length)];" },
  { "id": "code_24", "text": "function clamp(value, min, max) { return Math.min(max, Math.max(min, value)); }" },
  { "id": "code_25", "text": "const formatPercent = (value) => `${Math.round(value * 100)}%`;" },
  { "id": "code_26", "text": "addEventListener('keydown', (event) => { if (event.key.length !== 1) return; });" },
  { "id": "code_27", "text": "const area = document.createElementNS('http://www.w3.org/2000/svg', 'path');" },
  { "id": "code_28", "text": "const byTimestamp = sessions.slice().sort((a, b) => a.timestamp - b.timestamp);" },
  { "id": "code_29", "text": "const result = parts.filter(Boolean).join(' ').trim();" }
]
